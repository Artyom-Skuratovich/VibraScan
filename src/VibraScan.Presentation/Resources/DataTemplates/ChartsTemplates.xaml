<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:VibraScan.Application.VibrationMeasurements.Queries.GetCharts;assembly=VibraScan.Application"
                    xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
                    xmlns:conv="clr-namespace:VibraScan.Presentation.Resources.Converters">
    <Style x:Key="ChartLabelStyle" TargetType="TextBlock">

    </Style>

    <Style x:Key="ChartValueStyle" TargetType="TextBlock">

    </Style>

    <conv:TypeToVisibilityConverter x:Key="TypeToVisibilityConverter"/>
    <conv:ValuesToSeriesConverter x:Key="ValuesToSeriesConverter"/>

    <DataTemplate DataType="{x:Type local:BaseChart}">
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" x:Name="MetricsPanel">
                <TextBlock Text="RMS" Style="{StaticResource ChartLabelStyle}"/>
                <TextBlock Text="{Binding Rms}" Style="{StaticResource ChartValueStyle}"/>
            </StackPanel>

            <StackPanel x:Name="AmplitudePanel" Margin="40,0,0,0" Visibility="Collapsed">
                <TextBlock Text="TP-P" Style="{StaticResource ChartLabelStyle}"/>
                <TextBlock Text="{Binding (local:FrequencyDomainChart.AmplitudeRange)}" Style="{StaticResource ChartValueStyle}"/>
            </StackPanel>

            <TextBlock Grid.Row="1" Text="{Binding MeasurementDomain}"
                       HorizontalAlignment="Left" FontWeight="SemiBold" Margin="0,5"/>

            <lvc:CartesianChart Grid.Row="2"
                                Series="{Binding Values, Converter={StaticResource ValuesToSeriesConverter}}"/>

        </Grid>

        <DataTemplate.Triggers>
            <DataTrigger Binding="{Binding Converter={StaticResource TypeToVisibilityConverter}, ConverterParameter={x:Type local:FrequencyDomainChart}}"
                         Value="True">
                <Setter TargetName="AmplitudePanel" Property="Visibility" Value="Visible"/>
            </DataTrigger>
        </DataTemplate.Triggers>
    </DataTemplate>
</ResourceDictionary>