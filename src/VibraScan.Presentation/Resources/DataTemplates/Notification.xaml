<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:VibraScan.Presentation.ViewModels.Components">

    <DataTemplate DataType="{x:Type local:ToastNotificationViewModel}">
        <Border
            x:Name="NotifyBorder"
            Padding="15,10"
            Background="#F0F9F1"
            BorderBrush="#B7E1CD"
            BorderThickness="1"
            CornerRadius="8"
            Opacity="0"
            Visibility="Collapsed">

            <Border.Effect>
                <DropShadowEffect
                    BlurRadius="15"
                    Direction="270"
                    Opacity="0.1"
                    ShadowDepth="2" />
            </Border.Effect>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock
                    x:Name="MsgText"
                    Margin="0,0,12,0"
                    VerticalAlignment="Center"
                    FontSize="13"
                    FontWeight="Medium"
                    Foreground="#188038"
                    Text="{Binding Message}" />

                <Button
                    Grid.Column="1"
                    Background="Transparent"
                    BorderThickness="0"
                    Command="{Binding CloseCommand}"
                    Content="&#xE711;"
                    FontFamily="Segoe MDL2 Assets"
                    FontSize="10"
                    Opacity="0.6" />
            </Grid>
        </Border>

        <DataTemplate.Triggers>
            <DataTrigger Binding="{Binding IsSuccess}" Value="False">
                <Setter TargetName="NotifyBorder" Property="Background" Value="#FFF4F4" />
                <Setter TargetName="NotifyBorder" Property="BorderBrush" Value="#F9D7D7" />
                <Setter TargetName="MsgText" Property="Foreground" Value="#C5221F" />
            </DataTrigger>

            <DataTrigger Binding="{Binding IsVisible}" Value="True">

                <DataTrigger.EnterActions>
                    <BeginStoryboard>
                        <Storyboard>

                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="NotifyBorder" Storyboard.TargetProperty="Visibility">
                                <DiscreteObjectKeyFrame KeyTime="0:0:0" Value="{x:Static Visibility.Visible}" />
                            </ObjectAnimationUsingKeyFrames>

                            <DoubleAnimation
                                Storyboard.TargetName="NotifyBorder"
                                Storyboard.TargetProperty="Opacity"
                                To="1"
                                Duration="0:0:0.2" />
                        </Storyboard>
                    </BeginStoryboard>
                </DataTrigger.EnterActions>

                <DataTrigger.ExitActions>
                    <BeginStoryboard>
                        <Storyboard>

                            <DoubleAnimation
                                Storyboard.TargetName="NotifyBorder"
                                Storyboard.TargetProperty="Opacity"
                                To="0"
                                Duration="0:0:0.2" />

                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="NotifyBorder" Storyboard.TargetProperty="Visibility">
                                <DiscreteObjectKeyFrame KeyTime="0:0:0.2" Value="{x:Static Visibility.Collapsed}" />
                            </ObjectAnimationUsingKeyFrames>

                        </Storyboard>
                    </BeginStoryboard>
                </DataTrigger.ExitActions>
            </DataTrigger>
        </DataTemplate.Triggers>
    </DataTemplate>

</ResourceDictionary>