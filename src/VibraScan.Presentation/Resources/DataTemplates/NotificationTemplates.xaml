<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:VibraScan.Presentation.ViewModels.Models"
                    xmlns:system="clr-namespace:System;assembly=mscorlib">

    <DataTemplate DataType="{x:Type local:ToastNotification}">
        <Border x:Name="NotifyBorder"
                CornerRadius="8" BorderThickness="1" Padding="15,10"
                Background="#F0F9F1" BorderBrush="#B7E1CD"
                Opacity="0" IsHitTestVisible="True"
                Tag="{Binding IsVisible, Mode=TwoWay}">

            <Border.Effect>
                <DropShadowEffect BlurRadius="15" Direction="270" ShadowDepth="2" Opacity="0.1"/>
            </Border.Effect>

            <Border.RenderTransform>
                <TranslateTransform Y="20"/>
            </Border.RenderTransform>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock x:Name="MsgText" Text="{Binding Message}" 
                           Foreground="#188038" FontSize="13" FontWeight="Medium" 
                           VerticalAlignment="Center" Margin="0,0,12,0"/>

                <Button Grid.Column="1" Content="&#xE711;" FontFamily="Segoe MDL2 Assets" 
                        FontSize="10" Background="Transparent" BorderThickness="0" 
                        Command="{Binding CloseCommand}" Cursor="Hand" Opacity="0.6"/>
            </Grid>
        </Border>

        <DataTemplate.Triggers>
            <!-- Если ошибка -->
            <DataTrigger Binding="{Binding IsSuccess}" Value="False">
                <Setter TargetName="NotifyBorder" Property="Background" Value="#FFF4F4"/>
                <Setter TargetName="NotifyBorder" Property="BorderBrush" Value="#F9D7D7"/>
                <Setter TargetName="MsgText" Property="Foreground" Value="#C5221F"/>
            </DataTrigger>

            <!-- Анимации появления и автозакрытия -->
            <DataTrigger Binding="{Binding IsVisible}" Value="True">
                <DataTrigger.EnterActions>
                    <BeginStoryboard x:Name="ShowAnim">
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetName="NotifyBorder" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.2"/>
                            <DoubleAnimation Storyboard.TargetName="NotifyBorder" Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" To="0" Duration="0:0:0.3"/>

                            <!-- Таймер 5 секунд -->
                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="NotifyBorder" Storyboard.TargetProperty="Tag" BeginTime="0:0:5">
                                <DiscreteObjectKeyFrame KeyTime="0:0:0" Value="{x:Static system:Boolean.FalseString}"/>
                            </ObjectAnimationUsingKeyFrames>
                        </Storyboard>
                    </BeginStoryboard>
                </DataTrigger.EnterActions>
                <DataTrigger.ExitActions>
                    <StopStoryboard BeginStoryboardName="ShowAnim"/>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetName="NotifyBorder" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.3"/>
                        </Storyboard>
                    </BeginStoryboard>
                </DataTrigger.ExitActions>
            </DataTrigger>
        </DataTemplate.Triggers>
    </DataTemplate>

</ResourceDictionary>