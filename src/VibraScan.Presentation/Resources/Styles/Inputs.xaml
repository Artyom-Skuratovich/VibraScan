<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:h="clr-namespace:VibraScan.Presentation.Infrastructure.Helpers">

    <Style TargetType="TextBox">
        <Setter Property="FontFamily" Value="{StaticResource MainFont}"/>
        <Setter Property="FontSize" Value="{StaticResource FontSizeNormal}"/>
        <Setter Property="Background" Value="{StaticResource DefaultElementBackgroundBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource DefaultElementBorderBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="8,5,8,5"/>
        <Setter Property="SelectionBrush" Value="{StaticResource PrimaryActionHoverBrush}"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Grid>
                        <Border x:Name="Border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{StaticResource ControlCornerRadius}">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="0" />
                        </Border>

                        <Border x:Name="FocusedBorder"
                            BorderBrush="{StaticResource PrimaryActionBrush}"
                            BorderThickness="0,0,0,2"
                            CornerRadius="{StaticResource ControlCornerRadius}"
                            Visibility="Collapsed"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{StaticResource DefaultElementHoverBrush}"/>
                            <Setter TargetName="Border" Property="BorderBrush" Value="#999999"/>
                        </Trigger>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter Property="Background" Value="White"/>
                            <Setter Property="BorderBrush" Value="{StaticResource DefaultElementBorderBrush}"/>
                            <Setter TargetName="FocusedBorder" Property="Visibility" Value="Visible"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Background" Value="{StaticResource DisabledElementBackgroundBrush}"/>
                            <Setter Property="Foreground" Value="{StaticResource DisabledElementTextBrush}"/>
                            <Setter Property="Opacity" Value="0.6"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--<Style TargetType="TextBox">
        <Setter Property="FontFamily" Value="{StaticResource MainFont}"/>
        <Setter Property="FontSize" Value="{StaticResource FontSizeNormal}"/>
        <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}"/>
        <Setter Property="Background" Value="{StaticResource DefaultElementBackgroundBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource SubtleBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="5,2"/>
        <Setter Property="MinHeight" Value="32"/>
        <Setter Property="SelectionBrush" Value="{StaticResource PrimaryActionBrush}"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Grid>
                        <Border x:Name="TxtBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{StaticResource ControlCornerRadius}">

                            <Grid Margin="{TemplateBinding Padding}">

                                <TextBlock Text="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=(h:TextBoxHelper.Placeholder)}"
                                           Visibility="Hidden"
                                           IsHitTestVisible="False"
                                           VerticalAlignment="Center"/>

                                <ScrollViewer x:Name="PART_ContentHost"
                                              Margin="0"
                                              VerticalAlignment="Center"/>

                                <Border x:Name="FocusLine" 
                                        BorderBrush="{StaticResource PrimaryActionBrush}"
                                        BorderThickness="0,0,0,2"
                                        VerticalAlignment="Bottom"
                                        Margin="-5,0,-5,-3"
                                        Visibility="Collapsed"
                                        CornerRadius="0,0,4,4"/>
                            </Grid>
                        </Border>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="TxtBorder" Property="BorderBrush" Value="{StaticResource DefaultElementBorderBrush}"/>
                        </Trigger>

                        <Trigger Property="IsFocused" Value="True">
                            <Setter TargetName="TxtBorder" Property="BorderBrush" Value="{StaticResource PrimaryActionBrush}"/>
                            <Setter TargetName="FocusLine" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="TxtBorder" Property="Background" Value="White"/>
                        </Trigger>

                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="TxtBorder" Property="Background" Value="{StaticResource DisabledElementBackgroundBrush}"/>
                            <Setter TargetName="TxtBorder" Property="BorderBrush" Value="{StaticResource DisabledElementBorderBrush}"/>
                            <Setter Property="Foreground" Value="{StaticResource DisabledElementTextBrush}"/>
                            <Setter TargetName="FocusLine" Property="Visibility" Value="Collapsed"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>-->

</ResourceDictionary>